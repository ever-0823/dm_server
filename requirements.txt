fastapi
uvicorn
python-multipart
python-dotenv
pymysql
bcrypt
paddlepaddle
paddleocr
# 用于运行后端接口测试。
pytest
# Qwen3 Embedding 本地推理。
torch>=2.6,<3
sentence-transformers>=5,<6
modelscope>=1.28,<2
# Docling 负责 PDF、Office 和 HTML 文档的结构化解析。
docling>=2.0,<3
# PostgreSQL 连接和 pgvector 类型适配。
psycopg[binary]>=3.2,<4
pgvector>=0.4,<1
